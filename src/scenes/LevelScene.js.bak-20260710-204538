export class LevelScene {
  constructor() {
    this.key = "LevelScene";
  }

  create() {
    // Mapa, ruta, oleadas y torres pendientes.
  }

  update() {
    // La lógica clínica no debe ejecutarse aquí por frame.
  }
}
