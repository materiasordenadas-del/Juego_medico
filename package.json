{
  "name": "atb-tower-defense",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8027",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8027",
    "test": "node --test tests/*.test.mjs",
    "validate:clinical": "node scripts/validateClinicalResolver.mjs",
    "validate:assets": "node scripts/validateKaykitAssets.mjs",
    "check": "npm test && npm run validate:clinical && npm run validate:assets && npm run build"
  },
  "dependencies": {
    "three": "0.166.1"
  },
  "devDependencies": {
    "vite": "5.4.19"
  }
}
