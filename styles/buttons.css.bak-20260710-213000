button {
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 9px 12px;
  color: inherit;
  background: var(--surface-raised);
  cursor: pointer;
}

button:hover {
  border-color: var(--accent);
}
